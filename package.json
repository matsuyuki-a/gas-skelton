{
  "name": "gas-skelton",
  "version": "1.0.0",
  "description": "Google Apps Script Skelton Code",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run build && clasp push && clasp open",
    "build": "shx mkdir -p lib && tsc && npm run copy",
    "copy": "shx cp appsscript.json lib",
    "clean": "shx rm -rf lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@rmn_e",
  "license": "CC0-1.0",
  "devDependencies": {
    "@google/clasp": "^2.2.1",
    "@types/google-apps-script": "0.0.54",
    "node-jq": "^1.9.1",
    "shelljs": "^0.8.3",
    "shx": "^0.3.2",
    "typescript": "^2.7.2"
  }
}
